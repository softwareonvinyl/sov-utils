require 'spec_helper'

describe Sov::Utils do
  it 'has a version number' do
    expect(Sov::Utils::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
